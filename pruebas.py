#Econtrar suma de combinación de numeros que de el resultado esperado

def suma_comb(lista, n):
    resultados = []
    otra = []
    modo = None
    for x in enumerate(lista):
        for x in enumerate(lista):
            if x: pass

    return resultados

print(suma_comb([1, 5, 3, 2], 6))
